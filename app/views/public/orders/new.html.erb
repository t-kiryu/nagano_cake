<h1>注文情報入力</h1>

<div>
  <%= form_with model: @xxx, url: orders_confirm_path, method: :post do |f| %>
    <div>
      <h3>支払方法</h3>
      <div>
        <%= f.radio_button :payment_method, Order.payment_methods.key(0) %>
        <%= f.label :payment_method, Order.payment_methods_i18n[:credit_card] %></br>

        <%= f.radio_button :payment_method, Order.payment_methods.key(1) %>
        <%= f.label :payment_method, Order.payment_methods_i18n[:transfer]%>
      </div>
    </div>
    <div>
      <h3>お届け先</h3>
      <div>
        <%= f.radio_button :address, "ご自身の住所" %>
        <%= f.label :address, "ご自身の住所" %>
        <%# ログインユーザーの住所表示 %>
      </div>
      <div>
        <%= f.radio_button :address, "登録済住所から選択" %>
        <%= f.label :address, "登録済住所から選択" %>
        <%# ログインユーザーの登録住所をリスト表示 %>
        <%# f.select :genre_id, options_from_collection_for_select(Genre.all, :id, :name) %>
      </div>
      <div>
        <%= f.radio_button :address, "新しいお届け先" %>
        <%= f.label :address, "新しいお届け先" %>
        <div>
          <%= f.label :"郵便番号(ハイフンなし)" %>
          <%= f.text_field :postal_code %>
        </div>
        <div>
          <%= f.label :住所 %>
          <%= f.text_field :address %>
        </div>
        <div>
          <%= f.label :宛名 %>
          <%= f.text_field :name %>
        </div>
      </div>
    </div>
  <% end %>

  確認画面へ進む
  <%# link_to "確認画面へ進む", orders_confirm_path, class: 'btn btn-primary btn-sm %>
</div>
