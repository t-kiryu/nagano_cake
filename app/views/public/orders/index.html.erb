<div>
  <h1>注文履歴一覧</h1>
</div>

<div>
  <table>
    <thead>
      <tr>
        <td>注文日</td>
        <td>配送先</td>
        <td>注文商品</td>
        <td>支払金額</td>
        <td>ステータス</td>
        <td>注文詳細</td>
      </tr>
    </thead>
    <tbody>
    <%# @orders.each do |order| %>
      <tr>
        <td></td>
        <td>
          〒<%# order.postal_code %></br>
          <%# order.address %></br>
          <%# order.last_name %><%# order.first_name %>
        </td>
        <td><%# order.cart_item.item %></td>
        <td><%# 送料 + 商品合計 %></td>
        <td><%# order.status %></td>
        <td><%# link_to "表示する", order_path(order.id), class: 'btn btn-primary btn-sm %></td>
      </tr>
    <%# end %>
    </tbody>
  </table>
</div>