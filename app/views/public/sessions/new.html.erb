<div class="container">
  <div class="row">
    <div class="col-12">
      <h3 class="mx-auto mt-5">ログイン</h3>
      <div class="col-12">
        <div class="form-group col-10 mt-4">
          <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
            <p>会員の方はこちらからログイン</p>
            <table class="table table-borderless table-sm">
              <tr>
                <td><%= f.label :メールアドレス %></td>
                <td><%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: "form-control"  %></td>
              </tr>
              <tr>
                <td><%= f.label :パスワード %></td>
                <td><%= f.password_field :password, autocomplete: "current-password", class: "form-control"  %></td>
              </tr>
              <tr>
                <td></td>
                <td><%= f.submit "ログイン", class: 'btn btn-primary' %></td>
              </tr>
            </table>
          <% end %>
        </div>
      </div>
      <div class="registration col-12 mt-4">
        <div class="form-group col-10 mt-4">
          <p>会員登録がお済みでない方</p>
          <p><%= link_to "こちら　", new_customer_registration_path %>から新規登録を行ってください。</p>
        </div>
      </div>
    </div>
  </div>
</div>