<h1>会員一覧</h1>

<table>
  <thead>
    <tr>
      <td>会員ID</td>
      <td>氏名</td>
      <td>メールアドレス</td>
      <td>ステータス</td>
    </tr>
  </thead>

  <tbody>
    <%# @customers.each do |customers| %>
    <tr>
      <td><%= customers.id %></td>
      <td><%# link_to @customers.last_name, admin_customer_path(customer.id) %></td>
      <td><%= customers.email %></td>
      <td><%= customers.is_deleted %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<div class="pagi-nation">
  <%# 1, 2, 3, Next >, Last >> の5つを作成 %>
  <%# ページネーション実装 %>
  <%# gem導入　⇒　gemファイル（Kaminari） %>
</div>